# 双 ToF 距离传感器接入方案

| 项 | 内容 |
|---|---|
| 文档编号 | DG-HW-002 |
| 版本 | 0.1 |
| 日期 | 2026-08-11 |
| 状态 | 方案已确认，待采购与真机验证 |
| 对应阶段 | 距离感知 / 高度校验 |
| 前置文档 | [ESP32-S3 N16R8 主控选型与开发板说明](./2-esp32-s3-n16r8-platform.md) |

本文记录 Desk Gateway 后续增加两颗 ToF（Time of Flight）距离传感器的硬件与软件方案。当前硬件尚未采购，本文只冻结选型、接线和验证边界，**不表示驱动已经实现或传感器已经通过真机验收**。

---

## 1. 方案结论

| 位置 | 选型 | 预期芯片 | 用途 | 结论 |
|---|---|---|---|---|
| 桌面右侧 | TOF050C | VL6180X | 测量桌面右侧与书架之间约 10 cm 或更短的距离 | 采用 |
| 桌面下方 | TOF400C | VL53L1X | 测量传感器到地面的距离，换算桌面垂直高度 | 采用 |
| 桌面下方备选 | TOF200C | VL53L0X | 2 m 标称量程，可用于普通距离显示 | 不采用 |

TOF200C 的标称 2 m 量程虽然覆盖桌子约 129 cm 的最大高度，但 VL53L0X 的实际有效距离会受到地面反射率、环境光和视场覆盖面积影响。垂直高度数据还会参与后续安全判断，因此保留余量更大、支持 ROI（Region of Interest）的 TOF400C。

`TOF050C`、`TOF200C`、`TOF400C` 是常见商家模块名称，不是 ST 官方器件型号。下单前必须让卖家确认实际芯片分别为 `VL6180X`、`VL53L0X`、`VL53L1X`，并确认模块引出了 `XSHUT/SHUT` 引脚。若实物芯片不同，不能直接沿用本文的驱动假设。

---

## 2. 测量目标

### 2.1 右侧短距离

TOF050C 水平安装在桌面右侧，朝向书架：

- 重点测量范围：约 `0–100 mm`。
- 用途：识别桌面右侧过于接近书架或障碍物的情况。
- 传感器光轴应尽量垂直于目标表面，避免只照到书架边缘。
- 极近距离、无回波或量程状态异常不能直接解释为“距离安全”。具体故障策略必须基于实物数据确定。

### 2.2 垂直高度

TOF400C 安装在桌板下方并垂直朝向地面：

```text
桌面实际高度
  = 传感器到地面的测量距离
  + 桌面上表面到传感器光学中心的固定安装偏移
```

约束：

1. 传感器视场内不能长期出现桌腿、线缆、主机箱、椅子、脚或其他遮挡物。
2. 固定安装后必须测量安装偏移，不能直接把传感器读数当作桌面高度。
3. 现有控制盒高度仍作为主要业务数据；ToF 高度先作为独立校验数据，完成全行程验证后再决定是否参与安全上限判断。

---

## 3. GPIO 与接线方案

### 3.1 已占用和禁止使用的 GPIO

| GPIO | 当前用途 / 约束 |
|---|---|
| GPIO4 / GPIO5 | 控制盒侧 CLK / DAT |
| GPIO6 / GPIO7 | 原厂控制面板侧 CLK / DAT |
| GPIO19 / GPIO20 | ESP32-S3 原生 USB |
| GPIO35 / GPIO36 / GPIO37 | N16R8 Flash / Octal PSRAM 内部占用，禁止外接 |
| GPIO43 / GPIO44 | UART0 调试 |
| GPIO48 | 板载 WS2812 RGB |

### 3.2 ToF 总线分配

两颗传感器共享一组新的硬件 I²C 总线：

| ESP32-S3 | TOF050C | TOF400C | 说明 |
|---|---|---|---|
| 3V3 | VCC | VCC | 使用 ESP32 开发板 3.3V |
| GND | GND | GND | 共地 |
| GPIO10 | SCL | SCL | 共享 I²C 时钟 |
| GPIO11 | SDA | SDA | 共享 I²C 数据 |
| GPIO12 | XSHUT / SHUT | — | 单独控制 TOF050C 启停与地址初始化 |
| GPIO13 | — | XSHUT / SHUT | 单独控制 TOF400C 启停与地址初始化 |
| 不接 | GPIO1 / INT | GPIO1 / INT | 第一版采用轮询，不使用中断 |

供电要求：

- 两个模块都由 ESP32 开发板的 `3V3/GND` 供电。
- **不得使用升降桌 RJ45 红线的 3.3V 为 ToF 模块供电。**
- 到货后先确认模块是否自带稳压、I²C 电平转换和上拉电阻，不按商品标题猜测电气参数。
- 两块模块的 I²C 上拉会并联。接线后应断电测量 `SCL/SDA` 到 `3V3` 的等效阻值，确认没有形成过强上拉。

---

## 4. I²C 地址冲突处理

VL6180X、VL53L0X 和 VL53L1X 常用的 7-bit I²C 默认地址均为 `0x29`（文档中也可能以 8-bit 写地址 `0x52` 表示），两颗模块不能以默认地址同时工作。

每次 ESP32 启动时按以下顺序初始化：

1. GPIO12、GPIO13 拉低，使两颗传感器都保持关断。
2. 只释放 TOF050C 的 XSHUT，等待启动完成。
3. 通过默认地址 `0x29` 连接 TOF050C，并将其运行地址改为 `0x30`。
4. 释放 TOF400C 的 XSHUT，等待启动完成。
5. 通过默认地址 `0x29` 连接 TOF400C，并将其运行地址改为 `0x31`。
6. 分别读取 `0x30` 和 `0x31`，确认两颗传感器在线。

地址修改通常不会跨掉电保存，因此每次启动都必须重复以上过程。任一步失败时应保持距离数据无效，不能生成虚假的安全距离或高度。

---

## 5. 软件接入计划

硬件到货后新增独立的距离传感器组件，避免把具体芯片驱动写进 `desk_core` 或 `yourdesk_v1`：

```text
TOF050C / VL6180X ─┐
                    ├─ distance_sensors ── 距离状态 ── desk_core / Web / BLE
TOF400C / VL53L1X ─┘
```

建议输出的最小状态：

| 字段 | 含义 |
|---|---|
| `right_clearance_mm` | 桌面右侧到书架的距离 |
| `right_clearance_valid` | 右侧距离是否有效 |
| `floor_distance_mm` | TOF400C 到地面的原始距离 |
| `tof_height_mm` | 加入安装偏移后的桌面高度 |
| `tof_height_valid` | ToF 高度是否有效 |
| `sample_age_ms` | 最近一次有效数据的年龄 |
| `range_status` | 传感器返回的量程 / 信号状态 |

第一版开发顺序：

1. 完成 I²C、XSHUT 和双地址初始化。
2. 只记录原始距离、量程状态和时间戳，不干预桌子运动。
3. 加入异常值过滤、连续样本确认和安装偏移校准。
4. 在 Web 状态接口和页面显示两颗传感器的数据与有效性。
5. 完成全行程、不同地面和障碍物测试后，再接入安全策略。

---

## 6. 安全策略边界

### 6.1 右侧防碰撞

后续允许配置最小安全间距。只有连续多个有效样本都低于阈值时，才停止或禁止继续上升；下降和 STOP 必须始终可用。

无回波、超量程、过期数据和传感器掉线需要单独标记。具体是否按“危险接近”处理，要根据 TOF050C 在极近距离下的真机输出决定，不能在硬件到货前写死。

### 6.2 高度校验

- 控制盒高度与 ToF 高度同时有效时，记录两者差值。
- ToF 高度连续稳定后才能用于安全判断，单次跳变不能触发错误校准。
- 若两路高度明显冲突，优先停止上升并报告异常，但不能自动覆盖持久化校准值。
- 童锁、STOP 和现有来源权限的优先级不因增加 ToF 而改变。

---

## 7. 到货检查清单

收到模块后，编码前先完成：

- [ ] 拍摄模块正反面和芯片丝印。
- [ ] 确认 TOF050C 实际芯片型号和 TOF400C 实际芯片型号。
- [ ] 确认两个模块都引出 XSHUT / SHUT。
- [ ] 确认模块允许使用 3.3V 供电和 3.3V I²C 电平。
- [ ] 断电测量 VCC、GND、SCL、SDA、XSHUT 的连通关系和上拉阻值。
- [ ] 单独连接每颗模块，确认默认地址及基础测距。
- [ ] 再连接双模块，验证 XSHUT 分配地址流程。
- [ ] 确认模块安装位置不会遮挡 USB、现有 RJ45 总线或 ESP32 天线。

---

## 8. 真机验收标准

### 8.1 基础验收

- 两颗传感器连续冷启动 20 次均能分配到 `0x30`、`0x31`。
- 传感器初始化失败不会影响 Web、REST、BLE、原厂总线和 STOP。
- 拔掉任意一颗传感器后，固件报告该传感器离线且不产生错误运动命令。

### 8.2 TOF050C

- 在预期的 10 cm 及更短距离内记录不同目标材质的有效率和抖动。
- 覆盖极近距离、目标倾斜、目标边缘和无目标情况。
- 安全阈值测试中不存在把无效数据误判为“距离充足”的情况。

### 8.3 TOF400C

- 覆盖桌子约 64–129 cm 全行程。
- 分别测试浅色地面、深色地面、地垫以及不同环境光。
- 完成安装偏移校准后，桌面高度显示误差目标为不超过 `±10 mm`。
- 升降过程中高度变化方向正确，不出现长时间冻结、反向跳变或单帧大幅跳变。

只有通过以上验收，ToF 数据才可以从“显示和日志”升级为“运动安全输入”。

---

## 9. 决策记录

| 决策 | 结论 | 日期 |
|---|---|---|
| 右侧短距传感器 | TOF050C，实际芯片到货后确认 | 2026-08-11 |
| 垂直高度传感器 | TOF400C，优先确认采用 VL53L1X | 2026-08-11 |
| TOF200C 是否替代 TOF400C | 不替代；2 m 标称量程对低反射地面的安全余量不足 | 2026-08-11 |
| ToF 总线 | GPIO10 / GPIO11，共享硬件 I²C | 2026-08-11 |
| XSHUT | GPIO12 / GPIO13，启动时解决默认地址冲突 | 2026-08-11 |
| 首版软件边界 | 先采集、显示和验证，不直接干预运动 | 2026-08-11 |

---

## 10. 参考资料

- ST VL6180X：https://www.st.com/en/imaging-and-photonics-solutions/vl6180x.html
- ST VL6180X 数据手册：https://www.st.com/resource/en/datasheet/vl6180x.pdf
- ST VL53L0X：https://www.st.com/en/imaging-and-photonics-solutions/vl53l0x.html
- ST VL53L0X 数据手册：https://www.st.com/resource/en/datasheet/vl53l0x.pdf
- ST VL53L1X：https://www.st.com/en/imaging-and-photonics-solutions/vl53l1x.html
- ST VL53L1X 数据手册：https://www.st.com/resource/en/datasheet/vl53l1x.pdf
- [ESP32-S3 N16R8 主控选型与开发板说明](./2-esp32-s3-n16r8-platform.md)
- [Desk Gateway 架构总览](./architecture/overview.md)
