# BLE 三客户端并发与配对设备管理第 2 轮审查报告

## 基本信息

- 审查时间：2026-08-13 00:42:04 CST
- 审查范围：固件 REST 契约、Web 配对设备管理、React Native REST Client 与设置页、Watch Client Info 与 Busy 错误映射、相关主机测试
- 对比基线：第 1 轮修复提交 `1520bb2`

## 检查项目

### 文档审查

- 逐项对照 REST 路径、HTTP 状态、`pending/failed`、异步刷新、120 秒配对窗口、三设备容量和客户端提示语。
- Web 与手机端都使用已认证 REST；BLE 控制链路下管理请求仍独立走 REST，符合设计。

### 代码审查

- 固件管理接口的 `200/202/404/409/500` 映射与客户端错误处理一致。
- Web 已在 `pending/failed` 时禁止全删，并在容量已满时禁用配对。
- Watch 与手机端都写 Client Info，并将 Busy `0x80` 保持为已连接状态。
- 手机端按钮状态存在两处与固件/Web 契约不一致的问题，详见下表。

### 测试审查

- REST Client 已覆盖查询、配对窗口、单删、全删、非法 ID 与固件错误映射。
- Bond 纯策略已覆盖文案、轮询、配置门禁；尚未覆盖删除冲突与容量按钮策略。
- Watch 已覆盖 Client Info 编码和 Busy 识别。

### 工程进度审查

- 功能进度记录完整，真实三设备硬件矩阵仍保持未完成。
- 本轮发现属于手机端细节缺陷，不改变“代码功能已实现、产品硬件验收待完成”的范围判断。

## 发现的问题

| 编号 | 严重程度 | 问题 | 影响 | 修复计划 | 修复状态 |
|---|---|---|---|---|---|
| R2-01 | 中 | 手机端列表出现 `pending` 或 `failed` 设备后，“删除全部”按钮仍可点击。 | 用户必然收到固件 `409 delete_conflict`，与 Web 的冲突门禁及逐台重试流程不一致。 | 提取删除冲突纯策略；存在冲突时禁用全删，并补充 `pending/failed/idle` 测试。 | 已修复 |
| R2-02 | 低 | Bond 容量已满且配对窗口关闭时，配对按钮虽然 `disabled`，但未应用禁用视觉样式。 | 按钮看似可操作却无响应，状态反馈不准确。 | 提取容量门禁策略，并让 `disabled` 属性、样式和按压反馈使用同一结果；补充测试。 | 已修复 |

## 修复结论

R2-01、R2-02 已完成修复：手机端现与 Web/固件共用相同的删除冲突和容量门禁语义，按钮逻辑、视觉状态和按压反馈保持一致。

修复后验证结果：

- 手机端 `npm run typecheck`：通过
- 手机端测试：36 项全部通过（新增 2 项策略测试）
- 文档中的手机端测试数量已同步更新
- 本轮状态：通过
