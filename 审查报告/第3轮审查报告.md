# BLE 三客户端并发与配对设备管理第 3 轮审查报告

## 基本信息

- 审查时间：2026-08-13 00:45:47 CST
- 审查范围：全部需求与技术文档、固件 BLE/Web 实现、React Native、watchOS、自动化测试、构建门禁、checklist 与工程进度
- 对比基线：需求实施前提交 `1b84c851` 至第 2 轮修复提交 `e4d18f4`

## 检查项目

### 文档审查

- REST 路径、Client Info、Busy `0x80`、连接容量、配对窗口、Bond 删除状态与代码逐项一致。
- README、BLE Profile、手机、Watch、工程进度与 bringup checklist 没有残留“单连接”或“尚未实现”描述。
- 手机测试数量已更新为 36 项；Watch 保持 14 项。
- 实施 checklist 的自动化条目均已回填，三台真机硬件矩阵仍保持未勾选。

### 代码审查

- 固件固定三槽位并使用 generation 防止旧断连事件误清新连接；广播、Notify 与 STOP 规则符合设计。
- 运动控制权只有一个 owner；Busy 不抢占，任意 STOP 可释放，owner 断连执行安全停止。
- Bond Store、NVS 匿名注册表、配对准入、Repeat Pairing、单删/全删及失败重试状态闭环一致。
- REST Handler 仅通过已认证入口访问；HTTP Handler 不等待 GAP 断连事件。
- Web、手机、Watch 的 Client Info、错误映射、轮询和按钮门禁与固件契约一致。
- 未发现需要额外重构的重复代码、异常处理遗漏或潜在回归。

### 测试审查

- ESP-IDF 版本：`ESP-IDF v6.0.2`。
- 固件全部 Host 测试通过，ESP32-S3 隔离构建成功。
- 固件镜像 `desk-gateway.bin` 剩余 18% app 分区空间。
- 手机端 `npm run typecheck` 通过，36 项测试全部通过。
- Watch `swift test` 14 项全部通过，通用 watchOS 无签名构建成功。
- `git diff --check` 通过。

### 工程进度审查

- 代码、文档、测试与工程进度对自动化完成范围的描述一致。
- 工作树仅保留用户原有的 `scripts/desk-preset.sh` 修改，本需求未纳入、未覆盖该文件。
- 未执行 push。

## 发现的问题

本轮未发现新的代码、文档、测试或工程进度问题。

| 编号 | 严重程度 | 问题 | 修复计划 | 修复状态 |
|---|---|---|---|---|
| R3-00 | 无 | 无新增问题 | 无 | 无需修复 |

## 未关闭的外部验收门禁

真实 iPhone、Apple Watch、Android 与升降桌三台并发安全矩阵需要相应设备、固件烧录和人在桌旁执行短行程测试，不能由主机自动化替代。该项不是本轮遗留代码缺陷，但在取得硬件证据前产品验收必须保持 NO-GO。

## 审查结论

第 3 轮审查通过，无需代码修复。自动化范围达到代码 GO；真实三设备硬件矩阵仍为最终产品验收门禁。
