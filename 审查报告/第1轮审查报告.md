# BLE 三客户端并发与配对设备管理第 1 轮审查报告

## 基本信息

- 审查时间：2026-08-13 00:37:38 CST
- 审查范围：需求文档、固件 BLE 会话与 Bond 管理、Web 管理接口与页面、React Native 客户端、watchOS 客户端、自动化测试及工程进度文档
- 对比基线：`1b84c851` 至 `0cea8164`

## 检查项目

### 文档审查

- 需求文档与技术设计中的三连接上限、单运动 owner、显式配对窗口、Bond 容量、匿名设备标识及安全删除语义均已映射到实现。
- README、BLE Profile、移动端、Watch、联调 checklist 与工程进度已经同步。
- 自动化已完成项与真实三设备硬件验收项保持分离；硬件验收仍明确标记为未完成。

### 代码审查

- 检查连接槽位与 generation 防复用、运动 owner 竞争、断连 STOP、Client Info、配对准入、Bond NVS 注册表、删除队列、管理快照及各客户端 Busy 降级。
- 重点检查在线删除、断连超时、NVS 保存失败与 Repeat Pairing 的异常路径。

### 测试审查

- 固件 Host 测试及 ESP-IDF v6.0.2 隔离构建通过。
- React Native TypeScript 检查与 34 项测试通过。
- Watch 14 项 Swift 测试与通用 watchOS 无签名构建通过。
- `git diff --check` 通过。

### 工程进度审查

- 自动化开发与验证项已经回填。
- 真实 iPhone、Android、Apple Watch 三设备并发矩阵尚无硬件证据，保持产品验收 NO-GO，不计入代码缺陷。

## 发现的问题

| 编号 | 严重程度 | 问题 | 影响 | 修复计划 | 修复状态 |
|---|---|---|---|---|---|
| R1-01 | 中 | Repeat Pairing 先删除 NimBLE Store 密钥，随后保存 Bond 元数据失败时直接返回，但运行时注册表仍保留旧记录。 | Store 与管理快照短期不一致，旧记录可能占用容量，且用户看不到可重试原因。 | 保留旧记录但将其切换为 `failed`，发布可见错误状态；复用单设备删除接口重试，重启后由 Store/NVS 对账清理。补充失败状态测试断言。 | 已修复 |

## 修复结论

R1-01 已完成修复：Repeat Pairing 的 NVS 保存失败路径现在会发布 `failed` 状态及可重试原因；失败记录可以再次进入 `pending`，其状态转换已有测试断言。

修复后验证结果：

- `idf.py --version`：`ESP-IDF v6.0.2`
- 固件全部 Host 测试：通过
- ESP32-S3 隔离固件构建：通过，`desk-gateway.bin` 剩余 18% app 分区空间
- 本轮状态：通过
